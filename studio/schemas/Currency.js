import { labelSingleton, referredToBy, shortDescription } from "./props";

export default {
  name: 'Currency',
  title: 'Currency',
  type: 'document',
  liveEdit: true,
  fields: [
    labelSingleton,
  ]
}