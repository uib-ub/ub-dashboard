import { labelSingleton, referredToBy, shortDescription } from "./props";

export const Currency = {
  name: 'Currency',
  title: 'Currency',
  type: 'document',
  liveEdit: true,
  fields: [
    labelSingleton,
  ]
}